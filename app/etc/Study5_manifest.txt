
        android:icon="@mipmap/ic_launcher"      //앱 아이콘 설정
        android:label="@string/app_name"        //앱 실행 시 윗부분타이틀
        android:roundIcon="@mipmap/ic_launcher_round"   //앱 아이콘 둥글게 할지
        android:supportsRtl="true"
        android:theme="@style/Theme.Test3">     // 테마 (?)

        // 처음 실행될 페이지 -> android:name=".MainActivity" Main 부터 실행
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        //res
        // layout : 이미지 모음
        // mipmap : 안드로이드 어플이미지 파일(해상도별)
        // values : 원하는 color들을 미리 선언가능
        // strings : 원하는 text미리 정의 @string/[변수명] 과 같이 사용
        // styles : custom 테마같은거 미리 정의 activity_main.xml split에서 테마 선택가능
